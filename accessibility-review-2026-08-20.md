# アクセシビリティ確認メモ（2026-08-20）

## 静的確認

ホームにはページ固有の `h1` が1つあり、主要セクションは `h2`、カード見出しは `h3` の階層で構成されている。ソース上、ホーム内の `img` 要素にはすべて `alt` 属性がある。装飾用のロゴ・シンボルは空のaltまたはaria-hiddenで扱っている。

FAQとモバイルメニューの操作要素はbuttonで、FAQには `aria-expanded`、モバイルメニューには開閉状態を示す `aria-expanded` と日本語の `aria-label` がある。スキップリンクも設置済みで、主要CTAはリンクテキストだけで目的が分かる。

## 表示確認

375px幅のスマートフォンで、閉じたモバイルメニューからリンクが漏れて表示される問題を確認し、CMS由来のリンク数に依存しないmax-height方式へ修正した。修正後はヒーロー、CTA、ロゴ、モバイルメニューボタンが正常に表示された。PCでは法人・個人入口、画像ブロック、ブログ・動画入口、FAQ、最終CTAまで縦方向の情報設計が崩れていないことを確認した。

## 手動受入で残る項目

キーボードだけでの全フォーカス順、実端末での色覚・コントラスト、実コンテンツの動画プレーヤー操作、公開CMS変更の実機保存は、ユーザー環境または実コンテンツを用いた受入確認として残している。未確認の導入実績、顧客レビュー、人物写真の権利情報を新規に作成してはいない。

## 2026-08-20 統合版のキーボード確認

公開トップでTabキーを1回押下し、先頭の「本文へ移動」スキップリンクに可視フォーカスが移ることを確認しました。主要ナビゲーションには、法人向け、個人向け、考え方、支援内容、プロフィール、ブログ、動画、無料相談という目的が判別できるリンク名が表示されています。

法人・個人ページ、資料・相談CTA、プロフィール補助CTAのリンク名とURLは公開画面で確認済みです。すべてのTab遷移、FAQの開閉、モバイルメニューのキーボード操作、実機でのコントラスト確認は最終受入として残します。

## CTA改修後の追加確認

`/personal` でTab操作を確認し、最初にEMULABOの戻りリンク、次に主CTAへ順にフォーカスが移ることを確認した。主CTAはアンカー要素としてキーボード操作可能であり、目的情報を含む相談URLを保持する。

## トップページのリンク・モバイルメニュー属性確認

公開トップでは、スキップリンク、法人向け、個人向け、考え方、支援内容、プロフィール、ブログ、動画、無料相談のリンク名を確認した。モバイルメニューボタンには初期状態で`aria-label="メニューを開く"`と`aria-expanded="false"`が付与され、FAQボタンの展開状態とは別に管理されている。

## モバイルメニュー開閉の追加観測

デスクトップ幅の公開トップでメニューボタンをプログラム操作したところ、直後の属性読み取りでは`aria-expanded`が初期値のまま返った。デスクトップでは対象メニューが非表示となるため、この観測だけでモバイル開閉の可否は判断せず、実装ソースとモバイル幅の表示確認を併用して判定する。

## モバイルメニューの参照関係

モバイルメニューボタンは`aria-controls="mobile-menu-panel"`、`aria-expanded="false"`、`aria-label="メニューを開く"`を持ち、参照先の`id="mobile-menu-panel"`が存在することを確認した。開閉対象を支援技術が識別できる構造になっている。

## モバイルメニューの開閉状態確認

描画更新後にメニューボタンを操作し、開いた状態では`aria-expanded="true"`、`aria-label="メニューを閉じる"`、パネルの`mobile-menu--open`クラスが揃うことを確認した。再操作後には`aria-expanded="false"`、`aria-label="メニューを開く"`、閉じたパネルクラスへ戻ることを確認した。

## 主要CTAのコントラスト確認

主要CTAは前景色`rgb(7, 25, 46)`、背景色`rgb(91, 169, 217)`で、計算したコントラスト比は6.84:1だった。通常テキストのAA基準4.5:1、および大きなテキストのAA基準3:1を満たすことを確認した。ヒーロー見出しとヘッダーは白文字で、暗い画像オーバーレイ上に配置されている。
