# EMULABO CMS 利用ガイド

## 管理画面へのアクセス

サイト管理者は `/admin` を開き、プロジェクト所有者のアカウントでログインします。一般ユーザーはコンテンツを閲覧できても、編集用APIや管理画面の保存操作にはアクセスできません。

### 最短でHPを編集する手順

管理者は、まず **`/admin`** を開きます。トップページの文章、改行、配置、画像、CTAはここで編集します。新しい固定ページやヘッダー／フッターリンクは **`/admin/pages`**、ブログ記事は **`/admin/blog`**、動画は **`/admin/videos`** を開きます。編集後は保存し、画面上部の **「公開サイトを確認する」** からPCとスマートフォンの表示を確認してください。

| 編集したい内容 | 開く場所 | 保存後の確認先 |
| --- | --- | --- |
| トップの文章・画像・CTA・FAQ | `/admin` | `/` |
| 法人／個人などの追加ページ、ヘッダー／フッターリンク | `/admin/pages` | `/{スラッグ}` |
| ブログ記事 | `/admin/blog` | `/blog` と `/blog/{スラッグ}` |
| 動画 | `/admin/videos` | `/videos` と `/videos/{スラッグ}` |

## 更新できる内容

`/admin` のコンテンツ編集では、ヒーローのコピーと相談URL、法人・個人への入口、法人向け資料CTA、個人向け初回体験CTA、実務経験の数値、3つの支援内容、事例、プロフィール、FAQ、最終CTAを更新できます。ヒーロー・事例・プロフィール・相談・研修・運用の画像は、**画像を選択** からアップロードするか、既存の公開URLを貼り付けて差し替えます。すべての変更を確認した上で、固定表示される **「変更を保存」** を押してください。

| 項目 | 更新時の注意 |
| --- | --- |
| 数値・事例 | 公開可能で根拠が確認できる情報のみを記載する。 |
| 相談URL | 実際に予約または問い合わせにつながるURLを入力する。 |
| 法人・個人入口 | 見出し、説明、リンク先、カードのリンク表示名を更新できる。リンク先の公開ページがあることを確認する。 |
| 法人向け資料CTA | 表示名と資料URLを更新できる。URLを空欄にするとトップページの資料リンクは表示されない。 |
| 個人向け初回体験CTA | 表示名とURLを更新できる。`/personal` の主CTAにも反映される。 |
| 画像 | 権利・人物利用の確認済みの素材を使う。JPEG、PNG、WebP、GIF（10MB以下）は **画像を選択** から直接アップロードでき、`/manus-storage/` のURLが自動入力される。既存の `https://` または `/manus-storage/` URLの貼り付けも利用できる。 |
| 代替テキスト | 画像の内容を短く説明する。事例・プロフィール・相談・研修・運用画像、追加ページ、ブログ、動画サムネイルで編集できる。 |
| FAQ | 実際の契約条件・対応範囲と異なる表現を避ける。 |

### 写真を変更する手順（AI初心者向け）

写真を変えるときは、画面に出る **「写真を選ぶ」→「この写真を使う」→「変更を保存」** の順に進めます。URL、ファイル形式、詳しい設定を知っている必要はありません。

| 手順 | すること | 確認すること |
| --- | --- | --- |
| 1. 写真を選ぶ | **写真を選ぶ** を押し、スマートフォンまたはパソコンから写真を1枚選ぶ。 | JPEG、PNG、WebP、GIFのいずれかで、10MB以下である。 |
| 2. この写真を使う | 表示された新しい写真を確認し、**この写真を使う** を押す。 | 顔や大事な部分が切れていないかを確認する。 |
| 3. ページを保存 | ページ下部の **変更を保存** を押す。 | 保存後に **公開サイトを確認する** を押し、PCとスマートフォンで表示を見る。 |

アップロードが完了すると新しい写真が大きく表示されます。**この写真を使う** を押すまで、編集内容には反映されません。写真の説明やURL入力、元の写真との比較は **詳しい設定・確認** を開いた場合だけ表示されます。選んだ写真をやめる場合は **元の写真に戻す** を押せます。ページ全体の保存を行うまで、公開サイトの写真は変わりません。写真の選択に失敗しても、それまで公開されている写真は置き換わりません。

画像欄には、用途ごとの「おすすめの写真」と「避ける写真」の例が表示されます。迷った場合は、文字より写真の主役が分かりやすく、顔や重要な場面が切れていない写真を選んでください。アップロードは管理者に限り、標準のフォーム送信で認証済みの保存APIへ送られ、ストレージへ保存されます。画像のファイル名、形式、容量を検証しますが、公開前には権利、人物利用、内容の正確性、PC・モバイルでのトリミングを必ず確認してください。

| 画像の場所 | おすすめの大きさ・比率 | 切れないための注意 |
| --- | --- | --- |
| ヒーロー画像 | 横1920 × 縦1080ピクセル以上、横長16:9 | 見出しが左側に重なるため、人物や大事な場面は右側か中央に置く。 |
| プロフィール画像 | 横1200 × 縦1500ピクセル以上、縦長4:5 | 顔や手元が中央寄りにある写真を選ぶ。 |
| 相談・研修・運用・事例・追加ページの画像 | 横1600 × 縦1000ピクセル以上、少し横長 | 人物や大事な物は中央付近に入れる。端にあると画面サイズによって切れる場合がある。 |
| ブログのアイキャッチ・動画のサムネイル | 横1600 × 縦900ピクセル以上、横長16:9 | 一覧カードでは上下が少し切れる場合があるため、主役は中央に入れる。 |

## 公開前の確認

保存後は、管理画面上部の **「公開サイトを確認する」** から表示を確認します。特に、PCとスマートフォンでの改行、ヒーロー画像のトリミング、相談ボタンの遷移先、事例・数値の事実性を確認してください。

## 追加ページとリンクの管理

`/admin/pages` の **「ページ・リンク管理」** から、追加ページとリンクを管理できます。ページは、タイトル、URLスラッグ、概要、本文、アイキャッチ画像、画像の代替テキスト、CTA、公開状態、ヘッダー掲載を編集できます。アイキャッチは画像を選択してアップロードするか、URLを貼り付けて変更します。本文は空行で段落を分けられます。ページを公開しなければ、そのURLを直接開いても一般の閲覧者には表示されません。

統合版では `/corporate` が法人・同業者向け、`/personal` が個人向けの専用ページです。両ページとも、タイトル・概要・本文・画像・公開状態はここで変更します。`/personal` の主CTAはトップの目的別CTAと共通管理するため、リンク表示名とURLは `/admin` の **「個人向け初回体験CTA」** を編集してください。`/corporate` の無料相談CTAはページ内のCTA欄で、資料CTAは `/admin` の **「法人向け資料CTA」** で管理します。

リンク設定では、表示名、URL、ヘッダー／フッターの表示場所、外部リンクかどうか、表示／非表示、並び順を管理できます。外部リンクは新しいタブで開きます。公開ページをヘッダーに載せる場合は、ページ編集で **「公開する」** と **「ヘッダーに掲載する」** の両方をオンにしてください。

## 改行とテキスト配置

テキスト入力欄では、**Enter** で改行位置を指定できます。追加ページの本文では、空行を1つ入れると新しい段落として表示されます。トップページのヒーローと最終CTAでは、**左寄せ・中央寄せ・右寄せ** を選ぶと、見出し、説明、CTA、補足の配置がまとめて変わります。

追加ページでは、**見出し・概要**、**本文**、**CTA** の配置を個別に選べます。文章量が多い本文は左寄せ、告知や短いメッセージは中央寄せなど、ページの目的に合わせて使い分けてください。


## ブログ記事の管理

`/admin/blog` の **ブログ管理** から、タイトル、スラッグ、概要、本文、アイキャッチ画像、代替テキスト、著者、公開日時、並び順を編集できます。アイキャッチは画像を選択してアップロードするか、URLを貼り付けて設定します。公開する場合だけ **「公開する」** をオンにして保存してください。公開記事は `/blog` の一覧と `/blog/{スラッグ}` の詳細ページに表示され、本文は空行で段落を分けて表示されます。

## 動画の管理

`/admin/videos` の **動画管理** から、タイトル、説明、動画URL、サムネイル画像、代替テキスト、公開日時、並び順を編集できます。サムネイルは画像を選択してアップロードするか、URLを貼り付けて設定します。配信方式は YouTube、Vimeo、直接動画URL、Manusストレージから選択します。YouTubeは `youtube.com` または `youtu.be`、Vimeoは `vimeo.com` のHTTPS URLだけを許可します。ストレージ動画は `/manus-storage/` で始まるURLを入力してください。公開動画は `/videos` の一覧と `/videos/{スラッグ}` の詳細ページに表示されます。

動画を公開する前に、埋め込みプレーヤーの再生、サムネイルの代替テキスト、権利確認、モバイルのアスペクト比を確認してください。動画ファイルの直接アップロードは現在の画像URL方式と同様に採用していないため、必要な場合は認証付きストレージアップロードとして別途実装します。


## ブログ・動画を追加する標準手順

ブログ記事は `/admin/blog` を開き、**新規作成**からタイトル、半角英数とハイフンのスラッグ、概要、本文、著者を入力します。本文は空行で段落を分け、アイキャッチ画像を使用する場合は **画像を選択** からアップロードするか、権利確認済みの `https://` または `/manus-storage/` URLを入力し、代替テキストを設定します。初回は **「公開する」オフ** のまま保存し、一覧から再度開いて編集内容を確認してください。公開する場合は公開日時を設定し、公開サイトで `/blog` と `/blog/{スラッグ}` を確認します。

動画は `/admin/videos` を開き、**新規作成**からタイトル、スラッグ、説明、配信方式、許可された動画URLを入力します。YouTube・VimeoはHTTPSの公式ドメイン、直接動画URLはHTTPS、Manusストレージは `/manus-storage/` から始まるURLだけを使用します。初回は公開オフで保存し、編集画面でURL・サムネイル・代替テキストを再確認した後、必要な場合だけ公開します。公開後は `/videos` と `/videos/{スラッグ}` をPC・スマートフォンの両方で確認します。

| 操作 | 保存状態 | 閲覧者への表示 |
| --- | --- | --- |
| 新規作成して公開オフで保存 | 下書き | 一般公開されない |
| 下書きを再編集して保存 | 下書き更新 | 一般公開されない |
| 「公開する」をオンにして保存 | 公開 | 一覧・詳細URLに表示 |
| 公開中にオフへ変更して保存 | 非公開化 | 一般公開から除外 |
| 一覧から対象を選択して編集 | 既存データ更新 | 保存後の状態に更新 |

コンテンツを登録していない段階でも、公開一覧は空状態と相談導線を表示します。未存在のスラッグは「見つかりません」、取得失敗時は「取得できませんでした」と表示され、一覧へ戻る導線を提供します。サンプル記事・サンプル動画を勝手に登録せず、公開コンテンツは実際に使用許可のある情報だけを入力してください。


## 法人・個人入口の編集範囲

ホームの「法人のお客様」「個人のお客様」入口は `/admin` の **「法人・個人への導線と目的別CTA」** で編集します。アイブロウ、見出し、説明文、リンク先、リンク表示名を変更できます。表示画像はCMSで管理する `workshopImageUrl` と `consultationImageUrl` を参照するため、管理画面の画像URL変更が入口カードにも反映されます。リンク先を変更した場合は、公開済みのページまたは実際の申込URLへ遷移することを必ず確認してください。

ホームのブログ・動画入口は固定の一覧導線です。コンテンツ本文・サムネイル・公開状態は `/admin/blog` と `/admin/videos` で管理します。記事・動画が未登録でも入口自体は表示され、公開ページ側の空状態メッセージから無料相談へ進めます。

## CTAの計測と目的別の引き継ぎ

ホーム、法人・個人ページ、ブログ、動画の主要CTAは、クリックした場所、目的（法人相談・個人初回体験・資料・コンテンツ）、対象をイベントとして記録します。外部の相談フォームへ遷移するURLにも同じ目的情報が付加されるため、フォーム側で対応できる場合は問い合わせ種別の初期表示や集計に利用できます。計測が利用できない閲覧環境でも、リンク遷移自体は妨げません。
