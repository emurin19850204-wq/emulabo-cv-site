# 写真差し替え簡略化計画

## 目的

EMULABO CMSにおける写真差し替えを、URL、ファイル形式、比較設定を意識しなくても完了できる操作へ簡略化する。管理者は各画像欄で **「写真を選ぶ」→「この写真を使う」→ページを保存** の最小3操作だけで、トップ、追加ページ、ブログ、動画の画像を更新できるようにする。

既存の管理者認証、ストレージアップロード、ファイル形式・10MB上限、画像プレビュー、代替テキスト、元画像へ戻す機能は維持する。詳細なURL入力や推奨サイズ・比較表示は、必要な場合だけ展開できる「詳しい設定」に格納し、通常操作から外す。

## 実装方針

| 項目 | 変更内容 |
| --- | --- |
| 通常画面 | 最初に大きな **「写真を選ぶ」** ボタン、現在画像、選択後の新しい画像、**「この写真を使う」** ボタンだけを表示する。 |
| 自動処理 | 写真を選ぶと、そのまま認証済みアップロードを開始し、完了後にプレビューと仮の代替テキスト候補を自動入力する。 |
| 最終保存 | 「この写真を使う」は選択内容を親編集フォームへ確定する。公開サイトを変更するのは、従来どおりページ全体の **「変更を保存」** を押した時点だけとする。 |
| 詳細設定 | URL入力、元画像との詳細比較、推奨サイズ、写真の選び方、代替テキストの手動修正は折りたたみ領域へ移す。 |
| 安全性 | 形式・容量チェック、アップロード失敗時の既存画像維持、保存前の状態表示、キーボード操作、代替テキスト入力を保持する。 |

## 実施手順

| フェーズ | 内容 | 完了条件 |
| --- | --- | --- |
| 1. 現状確認 | `ImageAssetField` を確認し、既存のアップロード・プレビュー・元に戻す・代替テキストの状態遷移を整理する。 | 既存機能を壊さない変更範囲が確定する。 |
| 2. UI簡略化 | 共通画像フィールドを、通常はワンタップ中心、詳細は折りたたみの構成へ変更する。 | AI初心者がURL入力なしで差し替え手順を理解できる。 |
| 3. 全編集画面への適用 | トップ、追加ページ、ブログ、動画の画像欄で同じ操作を利用できることを確認する。 | 画面ごとに操作方法が変わらない。 |
| 4. 検証 | 画像選択、アップロード、確定、保存、公開反映、元へ戻す、失敗時、キーボード・モバイルを検証する。 | 既存の安全性とアクセシビリティを維持したまま簡略化できる。 |
| 5. 文書化 | CMS利用ガイドの写真差し替え手順を、最小3操作に更新する。 | 管理者が画面と手順書だけで更新できる。 |

## 受入基準

管理者がURLを入力せず、写真を選択してアップロード完了後に **「この写真を使う」** を押し、最後にページを保存するだけで画像を反映できること。失敗時に既存の公開画像が消えないこと。追加ページ、ブログ、動画も同一操作で利用できること。PC・モバイル・キーボードで操作でき、画像の代替テキストを確認・修正できること。

## 前提・リスク

写真のアップロードとページ全体の保存は分離する。これにより、写真を選んだだけで公開サイトが変わることはない。自動生成する代替テキスト候補は、画像の意味を保証できないため、公開前に管理者が確認・修正できる状態を維持する。既存のストレージURL入力は、移行済み画像や高度な運用向けに残す。
