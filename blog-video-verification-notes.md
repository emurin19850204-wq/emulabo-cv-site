

## 2026-08-20 状態確認

- `/blog` は公開記事0件の空状態メッセージと「相談から最新情報を受け取る」導線を表示した。
- `/videos` は公開動画0件の空状態メッセージと「動画で相談内容を確認する」導線を表示した。
- `/blog/does-not-exist` は非同期読み込み後に「記事を取得できませんでした」「再読み込みする」「ブログ一覧へ戻る」を表示した。
- `/videos/does-not-exist` は「動画を取得できませんでした」「再読み込みする」「動画一覧へ戻る」を表示した。
- ブログ・動画管理画面はログイン済み管理者セッションで開き、新規作成フォームの入力項目と保存操作を確認した。実データの作成・公開はユーザーのコンテンツを勝手に登録しないため未実施。


## not-found状態の再確認

- APIの公開詳細取得を `undefined` ではなく `null` に正規化した後、`/blog/does-not-exist` は「記事が見つかりません」と「ブログ一覧へ戻る」を表示した。
- 同じ修正後、`/videos/does-not-exist` は「動画が見つかりません」と「動画一覧へ戻る」を表示した。
- これにより、取得失敗時の「取得できませんでした」と、正常な未存在時の「見つかりません」を区別できる。


## 追加設計の確認

ブログ管理画面と動画管理画面の双方で、「新規作成 → 公開オフで下書き保存 → 一覧から再編集 → 公開するをオンにして保存」という手順を画面上部に表示した。両画面の空状態、一覧からの選択編集、新規作成ボタンも確認した。

新規コンテンツの初期値は公開オフで、ブログはスラッグ・本文・著者・公開日時、動画は配信方式・動画URL・サムネイル・公開日時を編集できる。スラッグはDBの一意制約に加え、作成・更新前の重複検出を行い、日本語のエラーメッセージを返す。動画URLは配信方式ごとの許可条件をサーバー側で検証する。実データの登録・公開は、ユーザーの原稿と動画URLを受け取るまで実施しない。


## 最終検証範囲と制約

実コンテンツを勝手に登録しない方針のため、DBへの実記事・実動画の作成、公開、実動画の再生確認はユーザー原稿と動画URLの提供前には実施していない。代わりに、管理画面の新規作成フォーム、公開オフ初期値、公開／非公開トグル、一覧の空状態、公開詳細のnot-found、動画URL許可条件、スラッグ重複防止、管理者権限保護をコード・単体テスト・ブラウザ画面で確認した。実データによる公開反映とプレーヤー再生は、コンテンツ入力後に行う運用確認として残している。

一覧ページでは読込中のスピナー、空状態、エラー表示の分岐を確認し、詳細ページでは初期読込分岐と取得失敗・not-found分岐をコードとブラウザ結果で照合した。ブラウザの応答が高速なため詳細ページの読込中画面は一瞬で完了するが、実装上は一覧・詳細ともに独立したloading状態を持つ。

## 受入確認専用の仮ブログ記事

`acceptance-placeholder-blog`を「【受入確認用】仮ブログ記事：CMSの公開・編集テスト」として、公開オフの下書きで保存した。管理画面の一覧には表示され、`/blog/acceptance-placeholder-blog`のURL候補と「下書き」状態を確認した。本文・概要には実績、顧客情報、レビュー、サービス成果を含めないことを明記している。

## 受入確認専用の仮動画

`acceptance-placeholder-video`を「【受入確認用】仮動画：再生・公開テスト」として登録準備した。配信方式は直接動画URLで、公開サンプル動画のURLを使用する。説明には外部の公開サンプルであり、EMULABOの制作物・実績・お客様の声ではないことを明記した。保存時は公開オフの下書きとする。

`acceptance-placeholder-video`を公開オフの下書きとして保存した。管理画面の一覧には表示され、`/videos/acceptance-placeholder-video`のURL候補と「下書き」状態を確認した。次は仮記事・仮動画を一時公開し、公開サイトの一覧・詳細・再生を確認する。

ブログ管理画面を再読込し、仮ブログが一覧に「下書き」として残り、選択して再編集できる状態を確認した。

仮ブログの公開スイッチをオンにして保存したが、一覧表示は下書きのままだった。公開日時の未設定または公開スイッチの保存値を編集フォームとデータ層で確認する。

## 公開スイッチの再確認

公開チェックボックス自体は実ブラウザ上でオンに切り替わることを確認した。公開状態を切り替えた直後に保存を実行した最初の試行では、状態更新前の値が保存され、DBでは `isPublished=0` のままとなった。

公開チェックボックスがオンであることを画面上で確認してから保存を実行したところ、保存完了通知が表示され、管理画面の一覧は `公開中` に更新された。したがって、公開スイッチと保存処理の基本動作は確認済みであり、受入確認では切替直後ではなく、表示更新を確認してから保存する手順で検証を継続する。

公開サイトでは `/blog` の一覧に仮ブログのタイトルと「実績・サービス内容・お客様の声ではありません」という仮コンテンツの明記が表示された。続いて `/blog/acceptance-placeholder-blog` ではタイトル、概要、本文、著者、無料相談CTAが正しく表示され、公開URLの反映を確認した。再編集保存と再非公開の確認を引き続き実施する。

再編集では、著者欄を `EMULABO CMS受入確認（再編集確認済み）` に変更して保存した。保存完了通知が表示され、`/blog/acceptance-placeholder-blog` の詳細ページにも変更後の著者名が反映されたため、公開済み記事の再編集・再保存を確認した。

最後に公開スイッチをオフにし、表示更新を確認してから保存した。保存完了通知と一覧の `下書き` 表示を確認し、仮ブログを公開状態から受入確認用の下書きへ戻した。公開一覧からの消去確認は、動画の受入確認と合わせて実施する。

## 仮動画の公開切替

`acceptance-placeholder-video` を編集フォームで開き、公開スイッチをオンにして表示更新を確認した後に保存した。保存完了通知が表示され、管理画面の動画一覧は `公開中` に更新された。続いて `/videos` の公開一覧と `/videos/acceptance-placeholder-video` の再生確認を実施する。

公開サイトの `/videos` 一覧には仮動画のタイトルと、外部の公開サンプルでありEMULABOの制作物・実績・お客様の声ではない旨の説明が表示された。`/videos/acceptance-placeholder-video` ではタイトル、説明、動画プレーヤー、相談CTAがデスクトップ幅で表示された。再生開始・モバイル表示・再編集・非公開切替は継続して確認する。

初回に登録した Google Cloud Storage の公開サンプルURLは、ブラウザ上で `MEDIA_ELEMENT_ERROR: Format error`（動画要素の `readyState=0`）となり、プレーヤー枠は表示されるものの再生できなかった。この動画は受入確認専用の外部サンプルであるため、互換性のある公開MP4へ差し替えたうえで、再生確認をやり直す。

互換性のある公開サンプルMP4（`https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4`）へURLを差し替えて保存した。動画要素は `readyState=3`、エラーなし、長さ約5.1秒で読み込まれ、デスクトップ幅で再生ボタンを操作して花の映像が表示された。再生終了時には `currentTime=duration=5.055`、`ended=true`、`readyState=4` を確認したため、直接動画URL方式での実再生を確認した。モバイル幅と非公開切替の確認を続ける。

390×844pxの実モバイル幅では、`scripts/mobile-video-playwright.mjs` により動画要素の読込・再生・レイアウトを自動検証した。動画の幅は390px、右端も390pxで横方向のはみ出しはなく、高さ219.375pxで表示された。`readyState=3`、エラーなし、動画長5.055秒の状態から再生を開始し、`currentTime=0.158898`、`paused=false` を確認した。PC・モバイルともに仮動画の実再生が確認できたため、最後に再編集・非公開切替と公開一覧からの消去を確認する。

互換性のある公開MP4へのURL差し替えと保存は、公開済み動画の再編集確認も兼ねている。最後に公開スイッチをオフにし、表示更新を確認してから保存した。保存完了通知と動画一覧の `下書き` 表示を確認し、仮動画を受入確認用の下書きへ戻した。公開動画一覧からの消去確認を実施する。

## 公開トグル直後保存の同期対策

初回受入確認で、公開チェックを切り替えた直後の保存が古い状態を参照する可能性を確認した。管理画面では、ブログ・動画の選択中レコードをrefにも同期し、保存時はその最新refからペイロードを作るように修正した。

修正後、仮ブログの公開トグルをオンにし、表示更新を待たず同一操作内で保存を実行した。保存完了通知と管理画面一覧の `公開中` 表示を確認したため、直後保存でも `isPublished=true` が送信されることを確認した。検証後、仮ブログは再度非公開へ戻す。

同じ検証を仮動画にも実施した。公開トグルをオンにした直後の同一操作内で保存すると、保存完了通知と動画一覧の `公開中` 表示が確認できた。続いて公開トグルをオフにした直後の同一操作内で保存し、一覧が `下書き` に戻ることを確認した。これにより、ブログ・動画双方でトグル直後の公開・非公開保存が最新値を送信することを確認した。

最終状態では、仮ブログ・仮動画の両方を下書きへ戻した。公開サイトの `/blog` は「公開中の記事はまだありません」、`/videos` は「公開中の動画はまだありません」の空状態を表示した。DBでも `acceptance-placeholder-blog` と `acceptance-placeholder-video` の `isPublished=0` を確認し、仮コンテンツが公開サイトに残っていないことを確定した。
