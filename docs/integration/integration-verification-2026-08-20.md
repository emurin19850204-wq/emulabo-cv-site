# 統合サイト検証メモ（2026-08-20）

## 自動検証

| 確認項目 | 結果 | 備考 |
|---|---|---|
| 単体テスト | 合格 | `pnpm test` は3ファイル・9件が通過。CMSの法人／個人導線・資料CTAの既定値補完を含む。 |
| 本番ビルド | 合格 | `pnpm build` が通過。既存の `/manus-storage/` 画像についてのみViteの実行時解決に関する既知の注意表示がある。 |
| 法人・個人ページ登録 | 合格 | `corporate` と `personal` を公開済みの追加ページとして登録。 |

## 表示確認

| 画面 | PC 1280px | モバイル 390px | 結果 |
|---|---|---|---|
| `/` | 法人／個人入口、ブランドストーリー、法人サービス、FAQ、ブログ・動画導線を表示 | 既存の縦積み構成と固定相談CTAを表示 | 確認済み |
| `/corporate` | 見出し、画像、3サービス説明、相談CTAを表示 | 見出し・本文・CTAが幅内に収まり縦方向に表示 | 確認済み |
| `/personal` | 見出し、画像、対象・内容・提供方法、初回体験CTAを表示 | 見出し・本文・CTAが幅内に収まり縦方向に表示 | 確認済み |

## 未実施の受入確認

- 管理者ログイン済みの実ブラウザで、`/admin` の「法人・個人への導線と目的別CTA」を変更・保存し、公開トップに反映されることを確認する。
- 管理者画面の `/admin/pages` で `/corporate` と `/personal` の公開状態、本文、画像、CTAを変更・保存できることを確認する。
- 実際に使う無料相談・初回体験・資料URLが確定した後、それぞれの外部遷移を確認する。
- キーボードTab操作、フォーカス遷移、リンク名、コントラスト、モバイルメニュー開閉の手動受入を記録する。

## CMS実画面確認

| 管理画面 | 確認結果 |
|---|---|
| `/admin` | 「法人・個人への導線と目的別CTA」編集欄を確認。法人・個人それぞれのアイブロウ、見出し、説明、遷移先URL、リンク名と、資料／初回体験の表示名・URLが表示される。 |
| `/admin/pages` | `/corporate` と `/personal` が公開中の追加ページとして一覧に表示され、本文・画像・CTA・公開状態を編集できるページとして認識される。 |

## CTA公開確認

| CTA | 確認結果 |
|---|---|
| 個人向け初回体験CTA | `/personal` に「初回体験を申し込む」が表示され、CMSの個人向け初回体験URL（`https://emulabo.com/contact`）を参照することを確認。 |
| プロフィール補助CTA | ホームに「支援範囲・進め方を相談で確認する」が表示され、無料相談URLへ遷移するリンクとして確認。 |
| 法人・個人入口 | ヘッダーとホームの目的別カードに「法人向け」「個人向け」のリンクが表示されることを確認。 |

## 外部CTA到達性

| リンク先 | 結果 | 補足 |
|---|---|---|
| `https://emulabo.com/contact` | 到達確認済み | お問い合わせフォームを表示。送信操作は実施していない。 |
| 法人向け資料PDF | 到達確認済み | 公開PDFが19ページのドキュメントとして読み込まれることを確認。 |

> 資料内の実績・数値とホームCMSの数値は、指標の定義が異なる可能性があるため、公開前に事実確認を行い、最新の根拠に統一してください。
